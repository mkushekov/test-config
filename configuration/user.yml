server:
  port : 4047
eureka:
  instance:
    instance-id: user:${random.value}
    prefer-ip-address: true
  registryFetchIntervalSeconds: 5
  client:
    service-url:
      defaultZone : http://discUser:discPassword@localhost:4041/eureka/
spring:
  security:
    oauth2:
      client:
        provider:
          keycloak:
            authorization-uri: ${app.config.keycloak.url}/realms/${app.config.keycloak.realm}/protocol/openid-connect/auth
            jwk-set-uri: ${app.config.keycloak.url}/realms/${app.config.keycloak.realm}/protocol/openid-connect/certs
            token-uri: ${app.config.keycloak.url}/realms/${app.config.keycloak.realm}/protocol/openid-connect/token
            #            user-info-authentication-method: post
            user-info-uri: ${app.config.keycloak.url}/realms/${app.config.keycloak.realm}/protocol/openid-connect/userinfo
            user-name-attribute: preferred_username
        registration:
          gateway:
            provider: keycloak
            authorization-grant-type: authorization_code
            client-id: ${app.config.keycloak.client-id}
            client-secret: ${app.config.keycloak.client-secret}
            redirect-uri: http://localhost:4044/login/oauth2/code/gateway
            scope: openid
      resourceserver:
        jwt:
          issuer-uri: ${app.config.keycloak.url}/realms/${app.config.keycloak.realm}
          jwk-set-uri: ${app.config.keycloak.url}/realms/${app.config.keycloak.realm}/protocol/openid-connect/certs
  redis:
    host: ${REDIS_HOST:10.25.72.24}
    port: 6379
    password: 12345
  datasource:
    username: pltauser
    password : V$3pUz2eR
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:5432/plta_user_db?currentSchema=public
  jpa:
    show-sql: true
    generate-ddl: false
  flyway:
    baseline-on-migrate: true
    enabled: true
  rabbitmq:
    host: ${RABBITMQ_HOST:10.25.72.24}
    port: 5672
    username: admin
    password: admin123
  servlet:
    multipart:
      enabled: true
      max-file-size: 1000MB
      max-request-size: 1000MB

#keycloak
app:
  config:
    keycloak:
      url: http://10.25.72.24:4030/auth
      realm: plta-realm
      client-secret: EL3ERLfFCHIfshARAf5xskJxSFVUyHew
      client-id: plta-client-id
  gateway-url: http://10.25.72.24:4044/
  role:
    LISTENER_ID: 2

